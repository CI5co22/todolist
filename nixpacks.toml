[phases.setup]
nixPkgs = ["php81", "php81Extensions.intl", "php81Extensions.mbstring", "php81Packages.composer", "nginx"]

[phases.install]
cmds = ["composer install"]

[phases.build]
cmds = ["composer install --no-dev --optimize-autoloader"]

[start]
cmd = "php-fpm -D && nginx -g 'daemon off;'"